{
  "name": "lumen-studio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Fast local thumbnail studio: unlimited text layers, 1,900+ live Google fonts, generated light, macOS wallpaper backgrounds and profile cards.",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
